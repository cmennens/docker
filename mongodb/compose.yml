version: '3.8'

services:
  mongodb:
    image: mongo:latest
    container_name: mongo1
    ports:
      - "27017:27017"
    volumes:
      - mongo1_data:/data/db
      - mongo1_config:/data/configdb
    restart: always

volumes:
  mongo1_data:
    driver: local
  mongo1_config:
    driver: local
