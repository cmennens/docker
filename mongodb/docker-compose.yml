version: '3.8'

services:
  mongodb:
    image: mongo:latest
    container_name: mongo1
    ports:
      - "27017:27017"
    volumes:
      - mongo1_data:/data/db
    restart: unless-stopped

volumes:
  mongo1_data:
    driver: local
